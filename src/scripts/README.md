# Scripts

- `facility-counts.py` — scrapes live facility counts into `../../data/facility_counts.csv`
- `R/` — R pipeline (`EDA.R`, `Training.R`, `Predictions.R`) and `renv` project
- `recommender/` — Python recommender (`recommend-times.py`)
